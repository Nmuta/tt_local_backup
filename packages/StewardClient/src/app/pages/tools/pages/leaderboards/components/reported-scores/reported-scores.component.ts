import { Component } from '@angular/core';
import { BaseComponent } from '@components/base-component/base.component';

/** Displays the reported leaderboard scores tool. */
@Component({
  selector: 'reported-scores',
  templateUrl: './reported-scores.component.html',
  styleUrls: ['./reported-scores.component.scss'],
})
export class ReportedScoresComponent extends BaseComponent {}
