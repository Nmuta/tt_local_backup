/** A single console details entry. */
export interface WoodstockConsoleDetailsEntry {
  consoleId: string;
  isBanned: boolean;
  isBannable: boolean;
  deviceType: string;
  clientVersion: string;
}
