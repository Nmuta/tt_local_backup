/** A single console details entry. */
export interface SteelheadConsoleDetailsEntry {
  consoleId: string;
  isBanned: boolean;
  isBannable: boolean;
  deviceType: string;
  clientVersion: string;
}
