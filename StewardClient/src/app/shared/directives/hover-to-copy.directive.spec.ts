import { HoverToCopyDirective } from './hover-to-copy.directive';

describe('HoverToCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverToCopyDirective();
    expect(directive).toBeTruthy();
  });
});
