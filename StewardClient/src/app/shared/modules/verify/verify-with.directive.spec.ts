import { VerifyWithButtonDirective } from './verify-with.directive';

describe('VerifyWithButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyWithButtonDirective(null);
    expect(directive).toBeTruthy();
  });
});
