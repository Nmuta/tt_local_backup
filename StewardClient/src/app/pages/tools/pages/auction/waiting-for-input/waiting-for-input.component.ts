import { Component } from '@angular/core';

/** Renders a simple component that indicates we are waiting for input. */
@Component({
  templateUrl: './waiting-for-input.component.html',
  styleUrls: ['./waiting-for-input.component.scss'],
})
export class WaitingForInputComponent {}
